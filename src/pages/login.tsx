import { Button } from "@components/ui/button";

const Landing = () => {
  return (
    <div className="w-full h-screen bg-[#fafafa] flex justify-center items-center">
      <Button>
        <h1 className="color-black text-5xl font-bold uppercase">Orion</h1>
      </Button>
    </div>
  );
};

export default Landing;
